
<%= form_for @coffee do |f| %>
invite blurb
  <%= f.text_field :invite_blurb %><br>
contact info 
  <%= f.text_field :invite_blurb_contact %><br>
<%= hidden_field_tag 'coffeemeet[invitee_id]', @member.id  %>
  <%= f.submit %>
<% end %>